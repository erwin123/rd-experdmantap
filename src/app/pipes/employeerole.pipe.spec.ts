import { EmployeerolePipe } from './employeerole.pipe';

describe('EmployeerolePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeerolePipe();
    expect(pipe).toBeTruthy();
  });
});
