import { StdservicerolePipe } from './stdservicerole.pipe';

describe('StdservicerolePipe', () => {
  it('create an instance', () => {
    const pipe = new StdservicerolePipe();
    expect(pipe).toBeTruthy();
  });
});
