import { AspekparentPipe } from './aspekparent.pipe';

describe('AspekparentPipe', () => {
  it('create an instance', () => {
    const pipe = new AspekparentPipe();
    expect(pipe).toBeTruthy();
  });
});
